const ErrorComponent = ({loadTimer1}) => {
  return (
    <div>
      <h1>Página no encontrada</h1>
      <h2>Regresando al inicio en: {loadTimer1} segundos ...</h2>
    </div>
  );
  };

export default ErrorComponent;
